const View = ({params}: { params: Promise<{ id: string }> }) => {
    return (
        <div className="h-auto w-full bg-white border border-gray-300 shadow-sm rounded-4xl p-10 flex flex-col gap-5">
            id
        </div>
    )
}

export default View